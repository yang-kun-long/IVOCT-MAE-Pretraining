{
  "permissions": {
    "allow": [
      "Skill(update-config)",
      "Bash(python -c \"import torch; import torchvision; import timm; from PIL import Image; import numpy as np; from tqdm import tqdm; print\\('Dependencies OK'\\)\")",
      "Bash(python *)",
      "Bash(gh *)",
      "Bash(git init *)",
      "Bash(git add *)"
    ]
  }
}
